{
  "version": 2,
  "builds": [
    { "src": "server/**/*", "use": "@now/node" },
    { "src": "src/**/*", "use": "@now/static-build" }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/api/(.*)", "dest": "server/index.js" },
    { "src": "/(.*)", "dest": "build/$1" }
  ]
}
